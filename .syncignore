###################################
# .syncignore
# 以下文件和目录将在编译执行成功后删除
# 避免同步到目标服务器
#
# 规则
# - 每行一个文件或目录
# - 空行和井号开头的行（就是支持 `#` 单行注释）会忽略
# - 行首、行尾的空格会剔除
# - 路径是相对于项目根目录的
# - 不支持模糊匹配，不支持部分匹配
#
# `src` 只能匹配 `/src`
# 不能匹配 `test/src`
# 也不能匹配 `/sr`
#
# 不支持通配符，下面的指定是不合法的
# src/*.ts
###################################


# 以下文件和目录将在编译执行成功后删除，避免同步到目标服务器

.vscode
.editorconfig
.eslintignore
.eslintrc
.gitignore
.nvmrc
.syncignore
nodemon.json
package-lock.json
tsconfig.json
# 日志目录
logs
# 源代码目录
src